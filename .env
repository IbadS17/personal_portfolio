# Copy this file to `.env` and fill in the values.
# Then restart the Vite dev server.

VITE_EMAILJS_SERVICE_ID=service_16xnoeu
VITE_EMAILJS_TEMPLATE_ID=template_jmteqem
VITE_EMAILJS_PUBLIC_KEY=1ac9wMp9oL4eEIrUX

# Instructions:
# 1) Create an account at https://www.emailjs.com/ and configure an email service (Gmail, SMTP, etc.).
# 2) Create an email template that uses variables: user_name, user_email, message.
# 3) Copy the service id, template id and public key into a local `.env` file (do NOT commit secrets).
# 4) Install the client package: `npm install @emailjs/browser`.
# 5) Restart the dev server and test the contact form; messages will be delivered to the email you configured in EmailJS.
